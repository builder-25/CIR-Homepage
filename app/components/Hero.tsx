"use client";

export default function Hero() {
  return (
    <section className="text-center mt-8 md:mt-16">
      <h2 className="text-3xl font-semibold text-neutral-900 tracking-tight">
        Drop your knowledge. We’ll orchestrate the rest.
      </h2>
      <span className="rounded-full bg-yellow-300 text-black text-[11px] font-semibold px-2 py-0.5">
        No confidential info; content should be suitable for public use.
      </span>
    </section>
  );
}
